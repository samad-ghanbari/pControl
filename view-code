 SELECT o.id,
    o.user_id,
    o.project_id,
    u.name,
    u.lastname,
    u.office,
    u.post,
    u.tel
   FROM pc.project_owner o
     LEFT JOIN pc.users u ON u.id = o.user_id
  ORDER BY u.lastname, u.name;
